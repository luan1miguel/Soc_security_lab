<group name="opnsense,firewall,port_scan,scanning">

	<rule id="110002" level="10" frequency="10" timeframe="60">
  		<if_matched_sid>110001</if_matched_sid>

  		<same_source_ip />

  	<description>Possível port scan detectado pelo OPNsense</description>

  	<group>
    		port_scan,
    		reconnaissance,
    		attack,
  	</group>
	</rule>
</group>
